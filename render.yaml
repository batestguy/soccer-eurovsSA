services:
  - type: web
    name: bayesian-world-cup-prediction
    runtime: python
    plan: free
    buildCommand: pip install -r spaces/requirements.txt
    startCommand: python spaces/app.py
    healthCheckPath: /
